public class TrackData
{
    public readonly string Name;
    public readonly float SongTimeOffset;
    public TrackData(string name, float timeOffset)
    {   
        Name = name;
        SongTimeOffset = timeOffset;
    }
}
